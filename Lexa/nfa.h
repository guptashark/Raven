#ifndef __NFA_H__
#define __NFA_H__

/* It is important to be able to 
simulate both a DFA and NFA automaton, 
since in practice, one might be faster 
than the other, and vice versa. Also, 
an NFA is a fundamental tool because 
we can convert regex into some NFA's 
without epsilon transitions. 
*/

struct nfa_state {

};

#endif
